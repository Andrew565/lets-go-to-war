import { StatisticsObject } from "./StatisticsObject";

export const parseStats = (HallOfStatistics: StatisticsObject[]) => {
  const numberOfStats = HallOfStatistics.length;
  const averageNumberOfTurns = HallOfStatistics.reduce
};
